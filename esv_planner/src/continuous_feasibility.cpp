#include "esv_planner/continuous_feasibility.h"

namespace esv_planner {

// This translation unit is kept so the continuous-feasibility layer has a
// dedicated implementation anchor as the rebuild grows beyond header-only
// defaults.

}  // namespace esv_planner
